from typing import List
class Solution:
     def maxDistance(self, grid: List[List[int]]) -> int:
          
          mapDistance = []
          maxDistance = 0
          for row in grid :
               for x in row :
                    pass

          return 0
        